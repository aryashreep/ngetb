language: node_js
node_js:
  - node
  - "8"
sudo: true
dist: trusty

branches:
  only:
  - master
  - /^(?i:feature)-.*$/

before_script:
 - export CHROME_BIN=/usr/bin/google-chrome
 - export DISPLAY=:99.0
 - sh -e /etc/init.d/xvfb start
 - sudo apt-get update
 - sudo apt-get install -y libappindicator1 fonts-liberation
 - wget https://dl.google.com/linux/direct/google-chrome-stable_current_amd64.deb
 - sudo dpkg -i google-chrome*.deb

script: 
  - ng test --watch false -cc
  - ng e2e
  - ng build --prod --build-optimizer --no-progress
  - angular-cli-ghpages  --branch=master --repo=https://${GITHUB_TOKEN}@github.com/aryashreep/aryashreep.github.io.git --name="aryashreep" --email=aryashree@etechbuddy.com

deploy:
  provider: pages
  skip_cleanup: false
  github_token: ${GITHUB_TOKEN} # Set in travis-ci.org dashboard
  local_dir: dist
  on:
    branch: master
